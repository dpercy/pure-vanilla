(module Example
    #;()

  (Builtin.debug "HELLO")
  (Builtin.debug ExampleLib.msg)

  ;(Builtin.debug (Lexer.lexAll "f(asdf,wer)"))

  (Builtin.debug (Parser.parseProgram "f(asdf,g(), 123, \"neato\")"))

  #;
  (Builtin.debug (Builtin.== (Parser.parseProgram "f(asdf,g(), 123, \"neato\")")
                             #'(f asdf (g) 123 "neato")))

  (Builtin.debug (Builtin.== #'(f asdf (g) 123 "neato")
                             #'(f asdf (g) 123 "neato")))


  ;;
  )
