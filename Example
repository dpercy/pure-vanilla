(module Example
    #;()

  (Builtin.debug "HELLO")
  (Builtin.debug ExampleLib.msg)

  (= prog "fn(f, g) -> f(asdf,g(), 123, \"neato\")")
  (Builtin.debug (Lexer.lexAll prog))

  (= ast (Builtin.first (Parser.parseProgram (Scope.setModname Scope.emptyScope "Example")
                                             prog)))
  (Builtin.debug ast)

  (= ast2 #'(lambda (f g) (f asdf (g) 123 "neato")))
  (Builtin.debug ast2)

  (Builtin.debug (Builtin.== ast ast2))


  ;;
  )
